#include <stdio.h>
/**
  * main - Entry Point
  *
  * Return: Always 0
  */
int main(void)
{
	int a, b;

	for (a = 0; c <= 98; a++)
	{
		for (b = a + 1; b <= 99; b++)
		{
			putchar((p / 10) + '0');
			putchar((p % 10) + '0');
			putchar(' ');
			putchar((q / 10) + '0');
			putchar((q % 10) + '0');
			if(p == 98 && q ==99 )
			{
				continue;
				putchar(',');
				putchar(' ');
			}	
		}
	}

	putchar('\n');
	return (0);
}

