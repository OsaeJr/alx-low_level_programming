#include "main.h"
/**
 * add -  adds two integers and returns the result.
 * @i: interger parameter
 * @a: interger parameters
 * Return: Always 0
 */
int add(int i, int a)
{
	int result = i + a;

	return (result);
}
