#include "main.h"
/**
 * print_alphabet - Prints alphabets
 * Return: Always 0
 */
void print_alphabet(void)
{
	char alphabet = 'a';

	for (alphabet = 'a'; alphabet <= 'z' alphabet++)
	{
		_putchar(alphabet);
	}
	_putchar('\n');
}
